# CLAUDE.md

This repository uses **[`AGENTS.md`](./AGENTS.md)** as the single source of truth for AI agent
guidance. Read it first.

`AGENTS.md` is a thin index that links into topic-scoped docs under [`docs/`](./docs) (architecture,
UI/domain/data layering, testing, tool lifecycle, resource isolation). Follow the link(s) relevant
to your task; do not duplicate those rules here.

> Don't add guidance to this file. Update `AGENTS.md` (or the doc it points to) instead, so there is
> exactly one place to maintain.
